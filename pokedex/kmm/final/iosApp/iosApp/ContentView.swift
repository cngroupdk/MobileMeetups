import SharedKit
import SwiftUI

struct ContentView: View {
    let greet = "Hello ios"

    var body: some View {
        Text(greet)
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
